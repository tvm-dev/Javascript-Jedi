/*
Logic Operators:
&& -> AND -> E
|| -> OR -> OU
! -> NOT -> NÃO
*/
//Tests
  //Two true expressions:
  //console.log(true && true); //All expressions need to be true!

  //Only one expression need to return true:
  //console.log(false || false || false || true);

   //invert the expression:
   //console.log(!false);
    // Deny twice the expression:
    console.log(!!true);

